<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles.css">
    <title>Edit the item</title>
</head>
<body>
    <%- include('../partials/_navbar.ejs') %>
    <h1>Edit <%= food.name %></h1>
    <form action="/users/<%= user._id %>/foods/<%= food._id %>?_method=PUT" method="post">
        <label for="name">Name: </label>
        <input value="<%= food.name %>" type="text" name="name" id="name"/>
        <button type="submit">Update the Item</button>
    </form>
</body>
</html>